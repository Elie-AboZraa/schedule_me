import 'package:flutter/material.dart';

class ContinuButton extends StatelessWidget {
  const ContinuButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}